name: 'Pull Request Labeler'
on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/labeler@v5
